// Deprecated ClerkProvider placeholder kept to avoid import crashes during migration.
// Remove all references to this component and delete this file once confirmed unused.
export const ClerkProvider = ({ children }: { children: React.ReactNode }) =>
  children as React.ReactElement | null;
